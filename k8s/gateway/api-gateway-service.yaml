apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: patient-management
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 4004
      targetPort: 4004
      nodePort: 30004
      protocol: TCP
